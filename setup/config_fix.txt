# Global LLM configuration
[llm]
model = "gpt-4o"
base_url = "https://api.openai.com/v1"
api_key = "YOUR_API_KEY"  # Make sure to replace this with your actual API key
max_tokens = 4096
max_input_tokens = 8192  # Set this to at least 8192 to avoid token limit errors
temperature = 0.0
api_type = "openai"

# Optional configuration for vision capabilities
[llm.vision]
model = "gpt-4o"
base_url = "https://api.openai.com/v1"
api_key = "YOUR_API_KEY"  # Same as above
temperature = 0.0

# Browser configuration
[browser]
headless = false
disable_security = true
extra_chromium_args = []
max_content_length = 2000

# Search settings
[search]
engine = "Google"
fallback_engines = ["DuckDuckGo", "Baidu"]
retry_delay = 60
max_retries = 3
